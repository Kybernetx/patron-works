---
layout: layouts/base.njk
title: "Blog"
description: "Weekly insights on technology, entrepreneurship, and building in public. Technical depth meets authentic storytelling."
---

<div class="page-content">
    <div class="container">
        <header class="page-header">
            <h1 class="page-title">{{ title }}</h1>
            <p class="page-description">{{ description }}</p>
        </header>
        
        {% if collections.posts.length > 0 %}
        <div class="posts-archive">
            <div class="posts-grid">
                {% for post in collections.posts %}
                <article class="post-card">
                    {% if post.data.image %}
                    <div class="post-image">
                        <img src="{{ post.data.image }}" alt="{{ post.data.title }}">
                    </div>
                    {% endif %}
                    <div class="post-content">
                        <div class="post-meta">
                            <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
                            {% if post.data.tags %}
                            <div class="post-tags">
                                {% for tag in post.data.tags %}
                                <span class="tag">{{ tag }}</span>
                                {% endfor %}
                            </div>
                            {% endif %}
                        </div>
                        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
                        <p>{{ post.data.description }}</p>
                        <a href="{{ post.url }}" class="read-more">Read More →</a>
                    </div>
                </article>
                {% endfor %}
            </div>
        </div>
        {% else %}
        <div class="no-posts">
            <h2>Coming Soon</h2>
            <p>The first post is in the works! Subscribe to the newsletter to be notified when new content is published.</p>
            <form class="newsletter-form">
                <input type="email" placeholder="Enter your email address" required>
                <button type="submit">Subscribe</button>
            </form>
        </div>
        {% endif %}
    </div>
</div> 